using UnityEngine;

/// <summary>
/// A marker component for voxel joints that allows for easier pivot editing in the inspector.
/// </summary>
public class VoxelJoint : MonoBehaviour
{
    // This component serves as a marker and handle for the custom editor.
}
